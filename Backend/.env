#Sobre o .evn, como ele nunca vai para o github,porconta dele possuir senhas sensiveis proprojeto, pra isso que vai servir o .gitignore na raiz do projeto
#ele vai pegar qualquer arquivo que tiver o nome .env ou *.env e vai ignorar ele.
# Servidor, a porta do react ja é a 3000,
# por isso vamo deixa o back na 3001 pra evita conflito entre élas
PORT=3001

# dados do banco de dados
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=
DB_NAME=psinx

# JWT, ele é usado pra criar os tokens de autenticação,
#que vai servir como uma senha mestra, que só o servidor vai saber e o segredo é uma string aleatória
JWT_SECRET=sindrome_xfragil_secret_key_2024 #mudar depois para a versao final
JWT_EXPIRES_IN=8h # esse vai ser o tempo do login ent dps de 8hrs o login expira e o medico precisa logar devolta
